import AddPostButton from '@components/AddPostButton'
import { styled } from '@mui/material'

export const StyledAddPostButton = styled(AddPostButton)({
  width: '100%',
  display: 'flex',
  justifyContent: 'space-between',
})
